class Engine::App : precompile {
  use List;
  use Engine::PaintInfo;
  use Eg::Node;
  use Engine::Renderer;
  
  has name : rw string;
  has index_document : rw Eg::Node;
  
  static method new : Engine::App() {
    my $self = new Engine::App;
    
    return $self;
  }
}
