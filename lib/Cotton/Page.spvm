package Cotton::Page {
  has path : rw string;
  has title : rw string;
  has body : rw Cotton::Node;
  
  sub new : Cotton::Page () {
    my $page = new Cotton::Page;
    
    return $page;
  }
}
