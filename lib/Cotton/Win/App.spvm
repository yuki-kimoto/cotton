package Cotton::Win::App {
  use SPVM::Unicode;
  
  has title : rw string;
  
  sub new : Cotton::Win::App() {
    return new Cotton::Win::App;
  }
  
  native sub run : void ($self : self);
}
