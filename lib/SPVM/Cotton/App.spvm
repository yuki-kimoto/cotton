class Cotton::App : precompile {
  use List;
  use Cotton::PaintInfo;
  use Eg::Node;
  use Cotton::Renderer;
  
  has name : rw string;
  has current_page : rw Eg::Node;
  
  static method new : Cotton::App() {
    my $self = new Cotton::App;
    
    return $self;
  }
}
