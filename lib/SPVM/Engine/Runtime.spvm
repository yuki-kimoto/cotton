class Engine::Runtime {
  
  # Fields
  has api : rw Engine::API;
  
  # Class Methods
  static method new : Engine::Runtime ($options : object[] = undef) {
    my $options_h = Hash->new($options);
    
    my $self = new Engine::Runtime;
    
    return $self;
  }
  
}
