# Copyright (c) 2024 Yuki Kimoto
# MIT License

class Eg::Document extends Eg::Node {
  
  has title : rw string;
  
  # Class Methods
  static method new : Eg::Document ($root_node : Eg::Node) {
    
    my $self = new Eg::Document;
    
    $self->init(Eg::Node->TYPE_DOCUMENT_NODE, [$root_node]);
    
    return $self;
  }
  
  method document_element : Eg::Node () {
    my $root_node = (Eg::Node)$self->child_nodes_list->get(0);
    
    return $root_node;
  }
  
  method head : Eg::Node () {
    my $root_node = (Eg::Node)$self->document_element;
    
    return (Eg::Node)$root_node->child_nodes_list->get(0);
  }
  
  method body : Eg::Node () {
    my $root_node = (Eg::Node)$self->document_element;
    
    return (Eg::Node)$root_node->child_nodes_list->get(1);
  }
  
}
