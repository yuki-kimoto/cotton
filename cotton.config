use strict;
use warnings;

use SPVM::Builder::Config;

my $config = SPVM::Builder::Config::Exe->new_c99(file => __FILE__);

my @libs = qw(
  gdi32
  stdc++
  d2d1
  Dwrite
  d3d11
  d3dcompiler
  dxgi
);

$config->add_lib(@libs);

$config;
