package Cotton::Win {
  use Cotton::Win::App;

  sub main : int ($args : string[]) {
    my $app = Cotton::Win::App->new;
    
    $app->set_title("コットン");
    
    $app->run;
  }
}
