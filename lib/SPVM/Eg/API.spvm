class Eg::API : interface_t {
  use Eg::Window;
  use Eg::Node;
  
  # Interface Methods
  method get_viewport_height : int ();
  
  method get_viewport_width : int ();
  
  method paint_node : void ($paint_info : object, $node : Eg::Node);
  
  method calc_text_height : int ($paint_info : object, $text : string, $width : int, $font_styles : string[]);
}
