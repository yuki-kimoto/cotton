class Eg::OS::Windows::API::Window {
  use List;
  use Eg::Node;
  use Eg::Runtime;
  
  # Fields
  has runtime : rw Eg::Runtime;
  
  has document : rw Eg::Node;
  
  static method new : Eg::OS::Windows::API::Window() {
    my $self = new Eg::OS::Windows::API::Window;
    
    return $self;
  }
}
